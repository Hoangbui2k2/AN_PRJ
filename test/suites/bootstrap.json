{
  "name": "bootstrap",
  "description": "TC-A02/TC-A03 (gateway online + heartbeat 60s) + TC-B01/TC-C01 (node 0x01 gửi data schema hợp lệ).",
  "timeout_seconds": 200,
  "steps": [
    {
      "name": "TC-A02 nhận gateway online khi connect",
      "action": "expect",
      "timeout": 60,
      "expect_msg": {
        "topic_regex": "/status$",
        "schema": "gw_status",
        "expect": {"status": "online"}
      }
    },
    {
      "name": "TC-A03 heartbeat #1 (60s)",
      "action": "expect",
      "timeout": 75,
      "expect_msg": {
        "topic_regex": "/status$",
        "schema": "gw_status",
        "expect": {"status": "online"}
      }
    },
    {
      "name": "TC-A03 heartbeat #2 (120s), nodes_registered=2",
      "action": "expect",
      "timeout": 75,
      "expect_msg": {
        "topic_regex": "/status$",
        "schema": "gw_status",
        "expect": {"nodes_registered": 2}
      }
    },
    {
      "name": "TC-B01/TC-C01: node 0x01 publish data schema hợp lệ",
      "action": "expect",
      "timeout": 60,
      "expect_msg": {
        "topic_regex": "node_01/data$",
        "schema": "data"
      }
    }
  ]
}